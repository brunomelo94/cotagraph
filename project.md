# Project 


My intent is to track spedings of the Brazilian deputies using data from differents source of the internet, including open data from anywhere, camara dos deputados API, TCU, and so on.

The objective is to have a frontend with interactive graph that connects entities, they beeing cities, states (receive money - edges), deputies (can receive maybe? but mainly sends money to other entities);

I want to track emendas, cota parlamentar, everything we can that deputies uses money from taxpayers citizens of Brazil.

I want IA agents to help me develop this app, and I have some decisions for tech stack and not so much from other techs.

I have two "trash apps" that I tried to build for my college project, they are old, must not be used as example, if such as "bad example" , only the ideias and/or data source might be usefull:

https://github.com/brunomelo94/server_cotagraph.git
https://github.com/brunomelo94/client_cotagraph.git

I also was developing and work for an MBA that was related, it is in:

https://github.com/brunomelo94/emendas-pix.git


I think you need to read these to create context. I mean we need a memory folder somewhere here so agents can easily identify what is going on, decisions, what are we working on, all the context we have, updated and without wrong facts.

The primary objective is to think of an app that has this information.

For the stack, I want so use (and learn):

Python (mmaybe some typescript, go and rust);
Websockets;
Redis;
NoSql; Graph database; SQL; Elasticsearch or something like that
Queue for syncing data (maybe something like bullmq, rabbit etc)
Angular and or React and maybe something cool with Python;
Some CID/CD Infra, cloud AWS lambda, google or azure - always aim for close to free!
Github, git, github actions;
Docker containers;
Claude code, Gemini, GPT etc
Security web and app
And many more you could think to help me
SOme machine learning insights using some technics of data science - if free some llm;

This is a project phase.

Should we start somewhere? GitHub PAT is configured via `gh auth login`.
